name: actions
run-name: this is run name
on:
  push:
    branches:
      - master
  # pull-request:
  #   - opened

jobs:
  clang_tidy_check:
    runs-on: self-hosted
    steps: 
      - uses: actions/checkout@v3
      - run: cd ${{github.workspace}} && mkdir build && cd build && cmake .. && make
      - run: ./trail
