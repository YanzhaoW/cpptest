name: actions
run-name: this is run name
on:
  push:
    branches:
      - master
  # pull-request:
  #   - opened

jobs:
  clang_tidy_check:
    runs-on: self-hosted
    container: 
      image: silkeh/clang:15-buster
    steps: 
      - uses: actions/checkout@v3
      - name: build&run
        run: |
          cd ${GITHUB_WORKSPACE}
          mkdir build
          cd build
          cmake .. 
          make
          ./trail
