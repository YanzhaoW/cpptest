#include "class1.hpp"
#include <array>
#include <chrono>
#include <iostream>
#include <string>
#include <vector>

auto main() -> int
{
    // auto vec = std::vector<int>(10);
    // for (int i = 0; i < vec.size(); i++)
    // {
    //     std::cout << vec[i] << std::endl;
    // }
    // int i;
    return 0;
}
